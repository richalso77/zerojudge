#include<stdio.h>
#include<string.h>


int main(){
    int m, n;
    int ans[102][102];
    int x;
    while (scanf("%d%d", &m, &n) != EOF){
        for (int i = 0; i < m; i++){
            for (int j = 0; j < n; j++){
                scanf("%d", &x);
                ans[i][j] = x;
            }
        }

        for (int i = 0; i < n; i++){
            for (int j = 0; j < m; j++){
                printf("%d ", ans[j][i]);
            }
            printf("\n");
        }
    }
    /*scanf("%d%d", &m, &n);
for (int i = 0; i < m; i++){
    for (int j = 0; j < n; j++){
        scanf("%d", &x);
        ans[i][j] = x;
    }
}

for (int i = 0; i < n; i++){
    for (int j = 0; j < m; j++){
        printf("%d ", ans[j][i]);
    }
    printf("\n");
}
    */
}
