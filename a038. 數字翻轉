#include<cstdio>

int main() {
	int x;
	int ans;
	while (scanf("%d", &x) != EOF) {
		ans = 0;
		while (x != 0) {
			ans = ans * 10;
			ans += x % 10;
			x = x / 10;
		}
		printf("%d\n", ans);
	}
	
	return 0;
}
